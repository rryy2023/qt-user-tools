# 文件命名规范更新说明

## ✅ 已完成的更新

### 1. 统一命名规范

所有打包文件现在使用统一的命名格式：

```
QiantuTroubleshooter_v{版本号}_{平台标识}.{扩展名}
```

### 2. 平台标识

- **macOS ARM64**: `macOS-ARM64`
- **macOS Intel**: `macOS-Intel`  
- **Windows**: `Windows-x64`

### 3. 文件类型

- **DMG**: `QiantuTroubleshooter_v0.0.1_macOS-ARM64.dmg`
- **ZIP**: `QiantuTroubleshooter_v0.0.1_macOS-ARM64.zip`
- **EXE**: `QiantuTroubleshooter_v0.0.1_Windows-x64.exe`

### 4. 修改的文件

1. **build_all_platforms.sh**
   - 添加 `get_output_name()` 函数
   - 更新 macOS ARM64/Intel 打包逻辑
   - 添加 ZIP 压缩包生成
   - 更新输出总结信息

2. **build_windows.py**
   - 添加命名规范函数
   - 自动重命名 EXE 文件
   - 自动创建 ZIP 压缩包

3. **build_windows.bat**
   - 更新输出信息，显示命名规范

## 📦 输出示例

运行 `./build_all_platforms.sh --all` 后：

```
dist/
├── mac_arm64/
│   ├── 千图网问题解决工具.app                    # 应用包（保持中文）
│   ├── QiantuTroubleshooter_v0.0.1_macOS-ARM64.dmg
│   └── QiantuTroubleshooter_v0.0.1_macOS-ARM64.zip
├── mac_intel/
│   ├── 千图网问题解决工具.app
│   ├── QiantuTroubleshooter_v0.0.1_macOS-Intel.dmg
│   └── QiantuTroubleshooter_v0.0.1_macOS-Intel.zip
└── windows/
    ├── QiantuTroubleshooter_v0.0.1_Windows-x64.exe
    └── QiantuTroubleshooter_v0.0.1_Windows-x64.zip
```

## 🔄 迁移旧文件

如果之前已经生成了旧命名的文件，可以手动重命名：

```bash
# macOS ARM64
cd dist/mac_arm64
mv "千图网问题解决工具_V0.0.1.dmg" "QiantuTroubleshooter_v0.0.1_macOS-ARM64.dmg"

# macOS Intel  
cd dist/mac_intel
mv "千图网问题解决工具_V0.0.1_Intel.dmg" "QiantuTroubleshooter_v0.0.1_macOS-Intel.dmg"
```

## 📝 使用说明

### 修改版本号

编辑 `build_all_platforms.sh` 中的 `VERSION` 变量：

```bash
VERSION="0.0.1"  # 修改为你的版本号
```

### 修改应用名称

编辑 `build_all_platforms.sh` 中的变量：

```bash
APP_NAME="千图网问题解决工具"        # 中文名称（用于 .app）
APP_NAME_EN="QiantuTroubleshooter"  # 英文名称（用于文件名）
```

## ✨ 优势

1. **清晰识别** - 文件名包含完整平台信息
2. **版本管理** - 版本号统一管理
3. **易于分发** - 用户一眼就能识别平台
4. **自动化** - 无需手动重命名

## 📚 相关文档

- `文件命名规范.md` - 详细的命名规范说明
- `打包说明_多平台.md` - 打包流程说明
