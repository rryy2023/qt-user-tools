# 修复说明：方法位置错误

## 🐛 问题

运行时错误：
```
AttributeError: 'MainWindow' object has no attribute 'on_problem_fix_clicked'
```

## 🔍 原因

`on_problem_fix_clicked` 和 `show_message` 方法被错误地放在了 `HostsCheckWorker` 类中，但它们应该是 `MainWindow` 类的方法。

## ✅ 修复

已将这两个方法移回 `MainWindow` 类：

```python
class MainWindow(QMainWindow):
    # ... 其他方法 ...
    
    def on_problem_fix_clicked(self, problem_type: str):
        """处理问题修复点击"""
        self.problem_fix_requested.emit(problem_type)
    
    def show_message(self, title: str, message: str, icon=QMessageBox.Icon.Information):
        """显示消息框"""
        msg = QMessageBox(self)
        msg.setIcon(icon)
        msg.setWindowTitle(title)
        msg.setText(message)
        msg.exec()


class HostsCheckWorker(QThread):
    # 只包含线程相关方法
    def run(self):
        # ...
```

## 🔧 需要重新打包

代码已修复，但需要重新打包应用：

### macOS ARM64

```bash
./build_all_platforms.sh --mac-arm64
```

### 或使用 GitHub Actions

```bash
# 提交修复
git add .
git commit -m "Fix: Move on_problem_fix_clicked to MainWindow class"
git push origin main

# 创建标签（自动打包）
git tag v0.0.2
git push origin v0.0.2
```

## ✅ 验证

修复后，应用应该可以正常启动，不再出现 `AttributeError`。
